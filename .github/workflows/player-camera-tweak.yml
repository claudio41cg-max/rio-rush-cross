name: Player camera tweak
on:
  push:
    paths:
      - '.github/workflows/player-camera-tweak.yml'
permissions:
  contents: write
jobs:
  tweak:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Bring camera slightly closer
        run: |
          sed -i 's/const CHASE_DISTANCE = 4.9;/const CHASE_DISTANCE = 4.55;/' src/game/FollowCamera.ts
          sed -i 's/const CHASE_HEIGHT = 2.15;/const CHASE_HEIGHT = 2.08;/' src/game/FollowCamera.ts
      - name: Validate
        run: |
          npm ci
          npm run build
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/game/FollowCamera.ts
          git diff --cached --quiet || git commit -m 'Make player RC slightly larger on screen'
          git push
